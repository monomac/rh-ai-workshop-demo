# Python deps the notebooks need on top of the RHOAI minimal-gpu:3.4 image.
# The minimal image ships JupyterLab + Python only; everything else we add here.
pandas>=2.2,<3
numpy>=1.26,<2
scikit-learn>=1.4,<2
matplotlib>=3.8,<4
mlflow>=2.16,<3
joblib>=1.4,<2
boto3>=1.34,<2
requests>=2.32,<3
