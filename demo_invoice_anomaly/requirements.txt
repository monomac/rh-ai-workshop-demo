# Top-up packages on the RHOAI 3.4 Standard Data Science Notebook image
# (odh-workbench-jupyter-datascience-cpu-py312-rhel9). That image already
# ships pandas, numpy, scikit-learn, matplotlib, boto3, requests, joblib —
# mlflow is the main one we still need to add for tracking + Model Registry.
mlflow>=2.16,<3
